请在 phpMyAdmin 中依次执行以下查询，并把结果告诉我：

1. 检查数据库是否存在：
   SHOW DATABASES LIKE 'fresh_harvest';

2. 切换到数据库：
   USE fresh_harvest;

3. 查看所有表：
   SHOW TABLES;

4. 如果有表，查看表的数量：
   SELECT COUNT(*) FROM information_schema.TABLES 
   WHERE table_schema = 'fresh_harvest';

请把这4个查询的结果截图或复制给我，我才能知道具体问题在哪里。

可能的情况：
A. 数据库不存在 → 需要创建数据库
B. 数据库存在但没有表 → 需要导入完整的数据库备份
C. 数据库存在，有部分表 → 可以运行修复脚本添加缺失的表和字段
D. 表名大小写问题 → MySQL 在不同系统上大小写敏感性不同

